version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: Import Variables
          run: |
            cat env0.auto.tfvars.json
            export DEBUG=1
            ../importVariables.sh
